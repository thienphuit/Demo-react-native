
import {ADD_TEXT,DELETE_TEXT} from '../constants/types';

let appState = [];
const inputtext = (state =appState,action)=>{
    switch(action.type){
        case ADD_TEXT:
            return {
                
            };
        case DELETE_TEXT:
            return{

            }    
    }
}
export default inputtext